# Transformer

Transformer 鐨勭壒鐐规槸锛?*瀹屽叏鍩轰簬娉ㄦ剰鍔涙満鍒讹紝涓嶅啀渚濊禆 RNN 鎴?CNN 鏉ュ鐞嗗簭鍒?*銆?
## 1. 鏁翠綋缁撴瀯

Transformer 浠嶇劧鏄竴涓?**缂栫爜鍣?瑙ｇ爜鍣ㄦ灦鏋?*锛?$婧愯瑷€鍙ュ瓙 -> Encoder -> 缂栫爜琛ㄧず -> Decoder -> 鐩爣璇█鍙ュ瓙$

Transformer Encoder 璐熻矗鐞嗚В杈撳叆鍙ュ瓙銆?$杈撳叆 鈫?Embedding 鈫?Encoder Block 脳 N$

Transformer Decoder 璐熻矗涓€姝ユ鐢熸垚杈撳嚭鍙ュ瓙銆?$杈撳嚭 鈫?Embedding 鈫?Decoder Block 脳 N 鈫?棰勬祴$

## 2. Encoder Block

姣忎釜缂栫爜鍣ㄥ潡鍖呭惈涓や釜瀛愬眰锛氬澶磋嚜娉ㄦ剰鍔涖€佸熀浜庝綅缃殑鍓嶉缃戠粶FFN銆傛瘡涓瓙灞傚闈㈤兘鏈塂ropout銆佹畫宸繛鎺ャ€丩ayerNorm銆?
缁撴瀯鏄細
$杈撳叆  X -> 澶氬ご鑷敞鎰忓姏 -> AddNorm -> FFN -> AddNorm -> 杈撳嚭$

#### 1) 澶氬ご娉ㄦ剰鍔?
娉ㄦ剰鍔涘眰璐熻矗璁╀笉鍚屼綅缃箣闂翠氦鎹俊鎭€?
杈撳叆锛?X$
鐢熸垚锛?
$$
Q=XW_{Q}
$$

$$
K=XW_{K}
$$

$$
V=XW_{V}
$$

璁＄畻锛?
$$
\mathbf{H}_{i}=softmax\left( \frac{\mathbf{Q}_{i}\mathbf{K}_{i}^T}{\sqrt{ d_{h} }} \right)\mathbf{V}_{i}
$$

$$
MultiHead(\mathbf{Q},\mathbf{K},\mathbf{V})=Concat(\mathbf{H}_{1},鈥?\mathbf{H}_{h})\mathbf{W}_{o}
$$

寰楀埌姣忎釜token铻嶅悎鍚庝笂涓嬫枃鐨勮〃绀恒€?
### 2) 娈嬪樊杩炴帴

$$
X+Attention(X)
$$

浣滅敤锛氫繚鐣欏師濮嬭緭鍏ヤ俊鎭€佽姊害鏇村鏄撻€氳繃娣卞眰缃戠粶浼犳挱銆?
### 3) Layer Normalization

鏌愪簺token缁村害闂存暟鍊煎樊寮傚緢澶э紝LayerNorm 璁╁潎鍊兼帴杩?锛屾柟宸帴杩?锛屽彲浠ョǔ瀹氭搴︺€?
$$
Y=LayerNorm(X+Attention(X))
$$

### 4) Feed Forward Network(FFN)

FFN 璐熻矗瀵规瘡涓綅缃殑琛ㄧず杩涜闈炵嚎鎬у彉鎹€?
缁撴瀯锛?Linear \to Activation \to Linear$

鍩轰簬浣嶇疆鐨勫墠棣堢綉缁?FFN锛氬垎鍒鐞嗘瘡涓猼oken鑷繁鐨勭壒寰佸彉鎹紝鑰屼笉鏄痶oken涔嬮棿鐨勪俊鎭氦娴?
$$
FFN(\mathbf{x})=\mathbf{W}_{2}\sigma(\mathbf{W}_{1}\mathbf{x}+\mathbf{b}_1)+\mathbf{b}_{2}
$$

鍏朵腑锛?- $\mathbf x$锛氭煇涓?token 鐨勯殣钘忚〃绀?- $\sigma$锛氭縺娲诲嚱鏁帮紝姣斿 ReLU

$$
Z=LayerNorm鈦?Y+FFN鈦?Y))
$$

杩欓噷锛?- $\mathbf Y$锛氱粡杩囪嚜娉ㄦ剰鍔涘悗鐨勪腑闂寸粨鏋?- $\mathbf Z$锛氱紪鐮佸櫒鍧楄緭鍑?- $\operatorname{FFN}$锛氬墠棣堢綉缁?
```python
class EncoderBlock(nn.Module):
    def __init__(self, key_size, query_size, value_size, num_hiddens,
                 norm_shape, ffn_num_input, ffn_num_hiddens,
                 num_heads, dropout, use_bias=False, **kwargs):
        super(EncoderBlock, self).__init__(**kwargs)

        self.attention = d2l.MultiHeadAttention(
            key_size, query_size, value_size,
            num_hiddens, num_heads, dropout, use_bias
        )

        self.addnorm1 = AddNorm(norm_shape, dropout)

        self.ffn = PositionWiseFFN(
            ffn_num_input, ffn_num_hiddens, num_hiddens
        )

        self.addnorm2 = AddNorm(norm_shape, dropout)

    def forward(self, X, valid_lens):
        Y = self.addnorm1(
            X,
            self.attention(X, X, X, valid_lens)
        )

        return self.addnorm2(Y, self.ffn(Y))
```

## 4. Transformer Encoder

Transformer 缂栫爜鍣ㄧ敱澶氫釜鐩稿悓鐨勭紪鐮佸櫒鍧楀爢鍙犺€屾垚銆?
$$
\mathbf{X}_{0}=Embedding(tokens)+\mathbf{P}
$$

$$
X_{l}=EncoderBlock_{l}(\mathbf{X}_{l-1})
$$

```python
class TransformerEncoder(d2l.Encoder):
    def __init__(
        self,
        vocab_size,
        key_size,
        query_size,
        value_size,
        num_hiddens,
        norm_shape,
        ffn_num_input,
        ffn_num_hiddens,
        num_heads,
        num_layers,
        dropout
    ):
        super().__init__()

        self.num_hiddens = num_hiddens

        self.embedding = nn.Embedding(
            vocab_size, num_hiddens
        )

        self.pos_encoding = d2l.PositionalEncoding(
            num_hiddens, dropout
        )

        self.blks = nn.Sequential()

        for i in range(num_layers):
            self.blks.add_module(
                f"block{i}",
                EncoderBlock(
                    key_size,
                    query_size,
                    value_size,
                    num_hiddens,
                    norm_shape,
                    ffn_num_input,
                    ffn_num_hiddens,
                    num_heads,
                    dropout
                )
            )
```

$$
\mathbf{X}=\sqrt{ d }鈰匛mbedding(\mathbf{x})+\mathbf{P}
$$

## 5. Decoder Block 瑙ｇ爜鍣ㄥ潡

瑙ｇ爜鍣ㄥ潡鏈変笁涓儴鍒嗭細鎺╄斀澶氬ご鑷敞鎰忓姏銆佺紪鐮佸櫒-瑙ｇ爜鍣ㄦ敞鎰忓姏銆佸墠棣堢綉缁?缁撴瀯锛?鐩爣搴忓垪杈撳叆 -> Masked Self-Attention -> AddNorm -> Encoder-Decoder Attention -> AddNorm -> FFN -> AddNorm -> 杈撳嚭$
瑙ｇ爜鍣ㄧ姸鎬侊細
`state = [ enc_outputs, enc_valid_lens, cache ]`

|浣嶇疆|鍐呭|
|---|---|
|`state[0]`|缂栫爜鍣ㄨ緭鍑簗
|`state[1]`|婧愬簭鍒楁湁鏁堥暱搴
|`state[2]`|姣忓眰瑙ｇ爜鍣ㄧ殑鍘嗗彶缂撳瓨|

### 1) 鎺╄斀鑷敞鎰忓姏Masked Self-Attention

$$
Q=XW_{Q}
$$

$$
K=XW_{K}
$$

$$
V=XW_{V}
$$

$$
Attention(Q,K,V)=softmax\left( \frac{QK^T+M}{\sqrt{ d }} \right)V
$$

- M锛氭槸 mask 鐭╅樀锛宻oftmax涔嬪悗瀵瑰簲浣嶇疆涓?.

### 2) Encoder-Decoder Attention(Cross Attention) 浜ゅ弶娉ㄦ剰鍔?
$$
\mathbf{Z}=AddNorm(\mathbf{Y},Attention(\mathbf{Q}=\mathbf{Y},\mathbf{K}=\mathbf{E},\mathbf{V}=\mathbf{E}))
$$

### 3) FFN鍓嶉缃戠粶

$$
\mathbf{O}=AddNorm(\mathbf{Z},FFN(\mathbf{Z}))
$$

```python
class DecoderBlock(nn.Module):
    def __init__(self, key_size, query_size, value_size, num_hiddens,
                 norm_shape, ffn_num_input, ffn_num_hiddens,
                 num_heads, dropout, i, **kwargs):
        super(DecoderBlock, self).__init__(**kwargs)
        self.i = i                                         # 褰撳墠鍧楃紪鍙?
        self.attention1 = d2l.MultiHeadAttention(...)      # 鎺╄斀鑷敞鎰忓姏

        self.addnorm1 = AddNorm(norm_shape, dropout)       # 绗竴涓?AddNorm

        self.attention2 = d2l.MultiHeadAttention(...)      # 缂栫爜鍣?瑙ｇ爜鍣ㄦ敞鎰忓姏

        self.addnorm2 = AddNorm(norm_shape, dropout)       # 绗簩涓?AddNorm

        self.ffn = PositionWiseFFN(...)                     # 鍓嶉缃戠粶

        self.addnorm3 = AddNorm(norm_shape, dropout)       # 绗笁涓?AddNorm
    def forward:
	    X2 = self.attention1(X, key_values, key_values, dec_valid_lens)

		Y = self.addnorm1(X, X2)

		Y2 = self.attention2(Y, enc_outputs, enc_outputs, enc_valid_lens)

		Z = self.addnorm2(Y, Y2)

		return self.addnorm3(Z, self.ffn(Z)), state
```

## 6. Transformer Decoder

瑙ｇ爜鍣ㄧ粨鏋勶細`Embedding + Positional Encoding -> DecoderBlock 1 -> DecoderBlock 2 -> ... -> DecoderBlock N -> Linear(vocab_size)`

```python
class TransformerDecoder(d2l.AttentionDecoder):
    def __init__(
        self,
        vocab_size,
        key_size,
        query_size,
        value_size,
        num_hiddens,
        norm_shape,
        ffn_num_input,
        ffn_num_hiddens,
        num_heads,
        num_layers,
        dropout
    ):
        super().__init__()

        self.num_hiddens = num_hiddens
        self.num_layers = num_layers

        self.embedding = nn.Embedding(
            vocab_size, num_hiddens
        )

        self.pos_encoding = d2l.PositionalEncoding(
            num_hiddens, dropout
        )

        self.blks = nn.Sequential()

        for i in range(num_layers):
            self.blks.add_module(
                f"block{i}",
                DecoderBlock(
                    key_size,
                    query_size,
                    value_size,
                    num_hiddens,
                    norm_shape,
                    ffn_num_input,
                    ffn_num_hiddens,
                    num_heads,
                    dropout,
                    i
                )
            )

        self.dense = nn.Linear(
            num_hiddens, vocab_size
        )
```

## 7. 璁粌

```python
num_hiddens = 32
num_layers = 2
dropout = 0.1
batch_size = 64
num_steps = 10

lr = 0.005
num_epochs = 200

ffn_num_input = 32
ffn_num_hiddens = 64
num_heads = 4

key_size = 32
query_size = 32
value_size = 32

norm_shape = [32]

train_iter, src_vocab, tgt_vocab = (
    d2l.load_data_nmt(batch_size, num_steps)
)

encoder = TransformerEncoder(
    len(src_vocab),
    key_size,
    query_size,
    value_size,
    num_hiddens,
    norm_shape,
    ffn_num_input,
    ffn_num_hiddens,
    num_heads,
    num_layers,
    dropout
)

decoder = TransformerDecoder(
    len(tgt_vocab),
    key_size,
    query_size,
    value_size,
    num_hiddens,
    norm_shape,
    ffn_num_input,
    ffn_num_hiddens,
    num_heads,
    num_layers,
    dropout
)

net = d2l.EncoderDecoder(
    encoder, decoder
)

d2l.train_seq2seq(
    net,
    train_iter,
    lr,
    num_epochs,
    tgt_vocab,
    device
)

enc_outputs = encoder(src, src_valid_lens)
dec_state = decoder.init_state(
    enc_outputs, src_valid_lens
)
output = decoder(tgt_input, dec_state)

# 棰勬祴
translation, attention_seq = d2l.predict_seq2seq(
    net,
    eng,
    src_vocab,
    tgt_vocab,
    num_steps,
    device,
    save_attention_weights=True
)

# 鐢?BLEU 璇勪环
d2l.bleu(
    translation,
    reference,
    k=2
)
```

