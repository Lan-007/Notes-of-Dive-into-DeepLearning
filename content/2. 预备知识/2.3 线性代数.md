# 绾挎€т唬鏁?
1. 鐐圭Н

```python
x = torch.tensor([0., 1., 2., 3.])
y = torch.ones(4)

torch.dot(x, y)
# tensor(6.)

torch.sum(x * y)
# tensor(6.)
```

2. 鐭╅樀-鍚戦噺涔樻硶

```python
A.shape  # (5, 4)
x.shape  # (4,)

torch.mv(A, x).shape
# (5,)
```

3. 鐭╅樀-鐭╅樀涔樻硶
(n, k) @ (k, m) 鈫?(n, m)

```python
A = torch.arange(20, dtype=torch.float32).reshape(5, 4)
B = torch.ones(4, 3)

A @ B
# 褰㈢姸鏄?(5, 3)
```

