# 娉ㄦ剰鍔涜瘎鍒嗗嚱鏁?
Query 鍜?Key 鍒板簳鎬庝箞鈥滃尮閰嶁€濓紵杩欎釜鐩镐技搴﹀垎鏁版€庝箞绠楋紵

## 1. 娉ㄦ剰鍔涜瘎鍒嗗嚱鏁?
娉ㄦ剰鍔涙満鍒?
$$
f(\mathbf{q},(\mathbf{k_{1}},\mathbf{v_{1}}),\cdots,(\mathbf{k_{m}}\mathbf{v_{m}}))=\sum_{i=1}^m\alpha(\mathbf{q},\mathbf{k_{i}})\mathbf{v_{i}}
$$

娉ㄦ剰鍔涜瘎鍒嗗嚱鏁帮細

$$
\alpha(\mathbf{q},\mathbf{k_{i}})
$$

娉ㄦ剰鍔涙潈閲嶄竴鑸敱 softmax 寰楀埌锛?
$$
\alpha(\mathbf{q},\mathbf{k_{i}})=\frac{\exp(\alpha(\mathbf{q,}\mathbf{k_{i}}))}{\sum_{j=1}^m\exp(\alpha(\mathbf{q},\mathbf{k_{j}}))}
$$

## 2. 娉ㄦ剰鍔涙祦绋?
绗竴姝ワ細鎵撳垎 $s_{i}=\alpha(\mathbf{q},\mathbf{k_{i}})$
绗簩姝ワ細softmax褰掍竴鍖?
$$
\alpha_{i}=softmax(s_{i})
$$

绗笁姝ワ細鍔犳潈姹傚拰

$$
\mathbf{o}=\sum_{i}\alpha_{i}\mathbf{v_{i}}
$$

## 3. masked softmax

鍦ㄨ嚜鐒惰瑷€澶勭悊涓紝涓€涓?batch 閲岀殑鍙ュ瓙闀垮害閫氬父涓嶄竴鏍枫€備负浜嗙粍鎴?batch锛岀煭鍙ュ瓙瑕佽ˉ padding锛?
```text
鍙ュ瓙 A锛氭垜 鐖?鏈哄櫒 瀛︿範
鍙ュ瓙 B锛氫綘濂?<pad> <pad> <pad>
```

浣嗘槸 `<pad>` 涓嶆槸鐪熷疄璇嶏紝妯″瀷涓嶅簲璇ュ叧娉ㄥ畠銆?鎵€浠ュ仛 softmax 鏃讹紝瑕佹妸 padding 浣嶇疆灞忚斀鎺夈€?
```python
def masked_softmax(X, valid_lens):                         # X 鏄敞鎰忓姏鍒嗘暟
										        # valid_lens 鏄瘡涓牱鏈殑鏈夋晥闀垮害
    if valid_lens is None:
        return nn.functional.softmax(X, dim=-1)

    else:                                                  # 濡傛灉浼犲叆浜嗘湁鏁堥暱搴?        shape = X.shape

        if valid_lens.dim() == 1:                          #  (batch_size,)
            valid_lens = torch.repeat_interleave(valid_lens, shape[1])

        else:                                              # 浜岀淮
            valid_lens = valid_lens.reshape(-1)            # 鎷夊钩鎴愪竴缁?
        X = d2l.sequence_mask(X.reshape(-1, shape[-1]), valid_lens,value=-1e6)

        return nn.functional.softmax( X.reshape(shape),  dim=-1)
```

鏃犳晥浣嶇疆濉?`-1e6` sofrmax鏉冮噸鎺ヨ繎0

## 4. 鍔犳€ф敞鎰忓姏

鍔犳€ф敞鎰忓姏閫傚悎query 鍜?key 缁村害涓嶄竴鏍风殑鎯呭喌銆傝瘎鍒嗗嚱鏁帮細

$$
\alpha(\mathbf{q},\mathbf{k})=\mathbf{w}_{v}^T\tanh(\mathbf{W}_{q}\mathbf{q}+\mathbf{W}_{k}\mathbf{k})
$$

- $\mathbf W_q$锛氭妸 query 鎶曞奖鍒伴殣钘忕┖闂?- $\mathbf W_k$锛氭妸 key 鎶曞奖鍒伴殣钘忕┖闂?- $\mathbf w_v$锛氭妸闅愯棌琛ㄧず鍙樻垚涓€涓爣閲忓垎鏁?- $\tanh$锛氶潪绾挎€ф縺娲诲嚱鏁帮紝澧炲姞妯″瀷琛ㄨ揪鑳藉姏

```python
class AdditiveAttention(nn.Module):                         # 瀹氫箟鍔犳€ф敞鎰忓姏灞?    def __init__(self, key_size, query_size, num_hiddens, dropout, **kwargs):
        super(AdditiveAttention, self).__init__(**kwargs)

        self.W_k = nn.Linear(key_size, num_hiddens, bias=False)
        self.W_q = nn.Linear(query_size, num_hiddens, bias=False)
        self.w_v = nn.Linear(num_hiddens, 1, bias=False)

        self.dropout = nn.Dropout(dropout)

    def forward(self, queries, keys, values, valid_lens):     # 鍓嶅悜浼犳挱
        queries = self.W_q(queries)
        keys = self.W_k(keys)

        features = queries.unsqueeze(2) + keys.unsqueeze(1)   # 涓や袱缁勫悎

        features = torch.tanh(features)

        scores = self.w_v(features).squeeze(-1)

        self.attention_weights = masked_softmax(scores, valid_lens)

        return torch.bmm(self.dropout(self.attention_weights), values)
```

## 5. 缂╂斁鐐圭Н娉ㄦ剰鍔?(Scaled Dot-Product Attention)

濡傛灉 query 鍜?key 鐨勭淮搴︿竴鏍凤紝灏卞彲浠ョ洿鎺ョ偣绉墦鍒嗐€傝瘎鍒嗗嚱鏁帮細

$$
\alpha(\mathbf{q},\mathbf{k})=\frac{\mathbf{q}^{T}\cdot\mathbf{k}}{\sqrt{ d }}
$$

- $d$ 鏄?query/key 鐨勭淮搴?- $\mathbf q^\top \mathbf k$ 鏄偣绉?- $\sqrt d$ 鏄缉鏀惧洜瀛?
濡傛灉 $d$ 寰堝ぇ锛岀偣绉細$\mathbf{q}^T\mathbf{k}$ 鍙兘鏁板€煎緢澶с€俿oftmax 杈撳叆杩囧ぇ鏃讹紝浼氬鑷村垎甯冪壒鍒皷閿愶細
	鏌愪竴涓綅缃潈閲嶆帴杩?1 锛屽叾浠栦綅缃帴杩?0

闄や互$\sqrt{ d }$ 鍙互鎺у埗鍒嗘暟澶у皬锛岃 softmax 鏇寸ǔ瀹氥€?
```python
class DotProductAttention(nn.Module):
    def __init__(self, dropout, **kwargs):
        super(DotProductAttention, self).__init__(**kwargs)

        self.dropout = nn.Dropout(dropout)

    def forward(self, queries, keys, values, valid_lens=None):
        d = queries.shape[-1]

        scores = torch.bmm(
            queries,
            keys.transpose(1, 2)
        ) / math.sqrt(d)

        self.attention_weights = masked_softmax(scores, valid_lens)

        return torch.bmm(self.dropout(self.attention_weights), values)
```

