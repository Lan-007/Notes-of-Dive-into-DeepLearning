# 鎯呮劅鍒嗘瀽

杈撳叆涓€娈甸暱搴︿笉鍥哄畾鐨勭數褰辫瘎璁猴紝鍒ゆ柇瀹冩槸绉瀬璇勮杩樻槸娑堟瀬璇勮銆?
```text
杈撳叆锛氬涓瘝鍏冩瀯鎴愮殑搴忓垪
杈撳嚭锛氭暣涓簭鍒楃殑涓€涓爣绛?浜屽垎绫?
```

浣跨敤IMDb鐢靛奖璇勮鏁版嵁闆嗭細
- 25000鏉¤缁冭瘎璁猴紱
- 25000鏉℃祴璇曡瘎璁猴紱
- 姣忎釜闆嗗悎涓銆佽礋鏍锋湰鏁伴噺鐩稿悓锛?- 鍥犳闅忔満鐚滄祴鐨勫噯纭巼绾︿负50%銆?
鏁版嵁鐩綍锛?
```text
aclImdb/
鈹溾攢鈹€ train/
鈹?  鈹溾攢鈹€ pos/   # 姝ｉ潰褰辫瘎
鈹?  鈹斺攢鈹€ neg/   # 璐熼潰褰辫瘎
鈹斺攢鈹€ test/
    鈹溾攢鈹€ pos/
    鈹斺攢鈹€ neg/
```

## 1. 鏁版嵁闆?
1. 璇诲彇鏁版嵁

```python
def read_imdb(data_dir, is_train):
    """璇诲彇IMDb璇勮鏁版嵁闆嗘枃鏈簭鍒楀拰鏍囩"""
    data, labels = [], []
    for label in ('pos', 'neg'):
        folder_name = os.path.join(
            data_dir,
            'train' if is_train else 'test',
            label
        )

        for file in os.listdir(folder_name):
            with open(os.path.join(folder_name, file), 'rb') as f:
                review = f.read().decode('utf-8').replace('\n', '')
                data.append(review)
                labels.append(1 if label == 'pos' else 0)

    return data, labels

train_data = read_imdb(data_dir, is_train=True)
```

2. 鍒嗚瘝涓庤瘝琛?
$鏂囨湰 鈫?token 鈫?token id 鈫?寮犻噺$

```python
train_tokens = d2l.tokenize(train_data[0], token='word') # 鎸夎瘝鍒囧垎

vocab = d2l.Vocab(train_tokens, min_freq=5, reserved_tokens=['<pad>'])
```

3. 鎴柇涓庡～鍏?
```python
num_steps = 500

train_features = torch.tensor([
    d2l.truncate_pad(vocab[line], num_steps, vocab['<pad>'])
    for line in train_tokens
])

print(train_features.shape)
# torch.Size([25000, 500])
```

4. 鍒涘缓DataLoader

```python
train_iter = d2l.load_array( (train_features, torch.tensor(train_data[1])), 64)
```

## 2. 浣跨敤寰幆绁炵粡缃戠粶(RNN)

鎸夐『搴忚瀹屾暣娈垫枃鏈紝鍐嶆彁鍙栨暣浣撳惈涔夈€?
$褰辫瘎 token id鈫?GloVe 璇嶅悜閲忊啋 鍙屽悜 LSTM鈫?搴忓垪琛ㄧず鈫掑叏杩炴帴灞傗啋 绉瀬 / 娑堟瀬$

1. 棰勮缁僄love

IMDb 璁粌闆嗗彧鏈?25,000 鏉″奖璇勶紝涓嶇畻鐗瑰埆澶с€傝嫢浠庨殢鏈鸿瘝鍚戦噺寮€濮嬭缁冿紙浠庨浂璁粌锛夛紝妯″瀷瀹规槗杩囨嫙鍚堬紝涔熶笉瀹规槗瀛﹀埌瓒冲濂界殑璇█鐭ヨ瘑銆傚師涔﹀姞杞藉湪澶ц妯¤鏂欎笂棰勮缁冪殑 GloVe锛?
```python
glove_embedding = d2l.TokenEmbedding('glove.6b.100d')
```

浣咷loVe鍙彁渚涘崟璇嶈〃绀猴紝杩樻病鏈夋彁渚涙暣鏉¤瘎璁虹殑琛ㄧず锛屽洜姝ら渶瑕佸弻鍚慙STM缁х画缂栫爜搴忓垪銆?
2. BiRNN妯″瀷

```python
class BiRNN(nn.Module):
    def __init__(self, vocab_size, embed_size, num_hiddens, num_layers):
        super(BiRNN, self).__init__()

        self.embedding = nn.Embedding(vocab_size, embed_size)

        self.encoder = nn.LSTM(
            embed_size,
            num_hiddens,
            num_layers=num_layers,
            bidirectional=True
        )

        self.decoder = nn.Linear(4 * num_hiddens, 2)
	# 鍙屽悜 LSTM
    def forward(self, inputs):
        embeddings = self.embedding(inputs.T)
        self.encoder.flatten_parameters()

        outputs, _ = self.encoder(embeddings)

        encoding = torch.cat((outputs[0], outputs[-1]), dim=1)

        outs = self.decoder(encoding)
        return outs
```

3. 鍒濆鍖栨ā鍨嬪強鏉冮噸

```python
embed_size = 100
num_hiddens = 100
num_layers = 2

devices = d2l.try_all_gpus()

net = BiRNN(
    len(vocab),
    embed_size,
    num_hiddens,
    num_layers
)

def init_weights(m):
    if type(m) == nn.Linear:
        nn.init.xavier_uniform_(m.weight)

    if type(m) == nn.LSTM:
        for param in m._flat_weights_names:
            if "weight" in param:
                nn.init.xavier_uniform_(
                    m._parameters[param]
                )

net.apply(init_weights)
```

4. 鍔犺浇骞跺喕缁?GloVe

```python
embeds = glove_embedding[vocab.idx_to_token]

net.embedding.weight.data.copy_(embeds)
net.embedding.weight.requires_grad = False
```

鍐荤粨鍚庝笉浼氫繚瀛樺拰鏇存柊姊害锛屼笉浼氭敼鍙樿繖浜涜瘝鍚戦噺锛孡STM鍜屽叏杩炴帴灞備粛姝ｅ父璁粌銆?
5. 璁粌

```python
lr, num_epochs = 0.01, 5

trainer = torch.optim.Adam(net.parameters(), lr=lr)
loss = nn.CrossEntropyLoss(reduction="none")

d2l.train_ch13(
    net, train_iter, test_iter,
    loss, trainer, num_epochs, devices
)
```

6. 灞€闄?
娌℃湁鏄惧紡澶勭悊鏈夋晥闀垮害锛屽彲鑳藉鐞嗗ぇ閲廯<pad>`銆丩STM闅句互骞惰

## 3. 浣跨敤鍗风Н绁炵粡缃戠粶(TextCNN)

鎵弿灞€閮ㄧ煭璇紝鍙戠幇灞€閮ㄦ儏鎰熸ā寮忋€?
$token id鈫?涓ゅ璇嶅悜閲忊啋 鍙樺涓€缁村嵎绉啋 鍏ㄥ眬姹犲寲锛氭瘡涓嵎绉牳鍙繚鐣欐渶寮哄搷搴斺啋 鎷兼帴鈫?鍏ㄨ繛鎺ュ垎绫?

1. TextCNN妯″瀷

```python
class TextCNN(nn.Module):
    def __init__(self, vocab_size, embed_size, kernel_sizes, num_channels):
        super(TextCNN, self).__init__()

        self.embedding = nn.Embedding(vocab_size, embed_size)
        self.constant_embedding = nn.Embedding(vocab_size, embed_size)

        self.dropout = nn.Dropout(0.5)
        self.decoder = nn.Linear(sum(num_channels), 2)

        self.pool = nn.AdaptiveAvgPool1d(1)
        self.relu = nn.ReLU()

        self.convs = nn.ModuleList()
        for c, k in zip(num_channels, kernel_sizes):
            self.convs.append(nn.Conv1d(2 * embed_size, c, k))

    def forward(self, inputs):
        embeddings = torch.cat((
            self.embedding(inputs),
            self.constant_embedding(inputs)
        ), dim=2)

        embeddings = embeddings.permute(0, 2, 1)

        encoding = torch.cat([
            torch.squeeze(self.relu(self.pool(conv(embeddings))), dim=-1)
            for conv in self.convs
        ], dim=1)

        outputs = self.decoder(self.dropout(encoding))
        return outputs
```

2. 鍔犺浇涓や唤Glove宓屽叆

```python
glove_embedding = d2l.TokenEmbedding(
    'glove.6b.100d'
)

embeds = glove_embedding[
    vocab.idx_to_token
]

net.embedding.weight.data.copy_(embeds)

net.constant_embedding.weight.data.copy_(
    embeds
)

net.constant_embedding.weight.requires_grad = False
```

3. 璁粌TextCNN

```python
lr = 0.001
num_epochs = 5

trainer = torch.optim.Adam(
    net.parameters(),
    lr=lr
)

loss = nn.CrossEntropyLoss(
    reduction='none'
)

d2l.train_ch13(
    net,
    train_iter,
    test_iter,
    loss,
    trainer,
    num_epochs,
    devices
)
```

