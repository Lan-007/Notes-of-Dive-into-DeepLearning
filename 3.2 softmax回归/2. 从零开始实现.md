#### 一、图像分类数据集

Fashion-MNIST 是服装图片分类数据集，共 10 类：
```
T 恤、裤子、套衫、连衣裙、外套、凉鞋、衬衫、运动鞋、包、短靴

训练集：60,000 张
测试集：10,000 张
每张图片：28 × 28 灰度图
类别数：10
```
##### 1.读取并转换图片
```python
import torchvision
from torchvision import transforms

trans = transforms.ToTensor()
train_data = torchvision.datasets.FashionMNIST(
	root = "./data",
	train=True,
	transform=trans,
	download=True
)
# (X, y)
test_data = torchvision.datasets.FashionMNIST(
	root="../data",
	train=False,
	transform=trans,
	download=True
)
# 原始图片通常是 PIL 图像，像素值范围是0-255
# 经过ToTensor()后：0.0-1.0, 单张图片形状：(1,28,28)
```
##### 2.DataLoader：小批量数据迭代器
```python
from torch.utils import data

batch_size = 256

train_iter = data.DataLoader(
	train_data,
	batch_size=batch_size,
	shuffle=True,
)
```
### 二、从零实现softmax回归

softmax是全连接线性模型，只接受一维特征向量，则需要展平图像为784(此时效果有限，因为忽略了”相邻像素通常相关“，CNN会解决这个问题)
##### 1.读取数据集
```python
import torch
from d2l import torch as d2l

batch_size = 256 
train_iter, test_iter = d2l.load_data_fashion_mnist(batch_size)
```
##### 2.初始化模型参数

前向传播：
```
X_flat:     (batch_size, 784)
W:          (784, 10)
b:          (10,)
logits:     (batch_size, 10)
```
$$\mathbf{O}=\mathbf{X}\mathbf{W}+b$$
```python
num_inputs = 784
num_outputs = 10

w = torch.normal(0, 0.01, size =(num_inputs, num_outputs), requires_grad=True)
b = torch.zeros(num_outputs, requires_grad=True)
```
##### 3.softmax函数
```python
def softmax(X):
	X_exp = torch.exp(X)
	partition = X_exp.sum(1, keepdim=True) 
	return X_exp/partition
```
##### 4.模型函数
```python
def net(X):
	return softmax(torch.matmul(X.reshape((-1, w.shape[0])), w) + b)
```
##### 5.交叉熵损失
```python
def cross_entropy(y_hat, y):
	return -torch.log(y_hat[range(len(y_hat)), y])
```
举例：
```python
y = torch.tensor([0, 2])

y_hat = torch.tensor([
    [0.1, 0.3, 0.6],
    [0.3, 0.2, 0.5]
])

y_hat[range(len(y_hat)), y] 等价于 y_hat[[0, 1], [0, 2]]
结果：[0.1, 0.5]
```
##### 6.准确率
```python
def accuracy(y_hat, y):
	if len(y_hat.shape) > 1 and y_hat.shape[1] > 1: # 类别大于1
		y_hat = y_hat.argmax(axis=1)                # 每行找最大值的位置
		
	cmp = y_hat.type(y.dtype) == y                  # 转换类型(eg:float转为int)
	return float(cmp.type(y.type).sum())            # 表示预测正确个数
```
##### 7.评估整个数据集
```python
def evaluate_accuracy(net, data_iter):
    """计算在指定数据集上模型的精度"""
    
    if isinstance(net, torch.nn.Module):
        net.eval()                  # 如果模型是PyTorch的nn.Module,就切换到评估模式

    metric = Accumulator(2)         # 两个累加器

    with torch.no_grad():
        for X, y in data_iter:
            metric.add(accuracy(net(X), y), y.numel())

    return metric[0] / metric[1]
```
##### 8.累加器
```python
class Accumulator:
    """在n个变量上累加"""
    
    def __init__(self, n):
        self.data = [0.0] * n

    def add(self, *args):
        self.data = [a + float(b) for a, b in zip(self.data, args)]

    def reset(self):
        self.data = [0.0] * len(self.data)

    def __getitem__(self, idx):
        return self.data[idx]
```
##### 9.训练一个epoch
```python
def train_epoch_ch3(net, train_iter, loss, updater):
    """训练模型一个迭代周期"""
    
    if isinstance(net, torch.nn.Module):
        net.train()

    metric = Accumulator(3)

    for X, y in train_iter:
        y_hat = net(X)                      # 前向传播
        l = loss(y_hat, y)                  # 计算loss

        if isinstance(updater, torch.optim.Optimizer):
            updater.zero_grad()
            l.mean().backward()
            updater.step()
        else:
            l.sum().backward()
            updater(X.shape[0])             # 反向传播、更新参数

        metric.add(float(l.sum()), accuracy(y_hat, y), y.numel()) # 

    return metric[0] / metric[2], metric[1] / metric[2]
```
##### 10.训练多个epoch
```python
def train_ch3(net, train_iter, test_iter, loss, num_epochs, updater):
    """训练模型"""
    
    animator = Animator(
        xlabel='epoch',
        xlim=[1, num_epochs],
        ylim=[0.3, 0.9],
        legend=['train loss', 'train acc', 'test acc']
    )

    for epoch in range(num_epochs):
        train_metrics = train_epoch_ch3(net, train_iter, loss, updater)
        test_acc = evaluate_accuracy(net, test_iter)
        animator.add(epoch + 1, train_metrics + (test_acc,))

    train_loss, train_acc = train_metrics

    assert train_loss < 0.5, train_loss
    assert train_acc <= 1 and train_acc > 0.7, train_acc
    assert test_acc <= 1 and test_acc > 0.7, test_acc
```
##### 11.优化方法：sgd
```python
lr = 0.1
def updater(batch_size):
    return d2l.sgd([W, b], lr, batch_size)
```