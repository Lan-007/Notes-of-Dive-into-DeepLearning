# 绠€娲佸疄鐜?
```text
鍑嗗鏁版嵁
鈫?瀹氫箟妯″瀷
鈫?瀹氫箟鎹熷け鍑芥暟
鈫?瀹氫箟浼樺寲鍣?鈫?璁粌寰幆
鈫?娴嬭瘯妯″瀷
```

## 1. 璁粌鏁版嵁

```python
true_w = torch.tensor([2, -3.4])
true_b = 4.2
features, labels = d2l.synthetic_data(true_w, true_b, 1000)
```

## 2. 灏忔壒閲忔暟鎹凯浠ｅ櫒:torch.uitls.data.DataLoader

```python
from torch.utils import data

def load_array(data_arrays, batch_size, is_train=True):
	"""鏋勯€犱竴涓猵ytorch鏁版嵁杩唬鍣?""

	dataset = data.TensorDataset(*data_arrays) # (featrues, labels)鎵撳寘鎴愭暟鎹泦

	return data.DataLoader(dataset, bartch_size, shuffle=is_train) # 姣忔寰幆杩斿洖涓€涓猙atch, 姣忎釜epoch寮€濮嬫椂闅忔満鎵撲贡鏁版嵁闆?```

## 3. 妯″瀷:nn.Linear

```python
from torch import nn

net = nn.Sequential(nn.Linear(2,1)) # 澶氬眰缃戠粶鍐欐硶
net = nn.Linear(2,1) # 鍗曡繛鎺ュ眰(2,1)
```

## 4. 鍒濆鍖栧弬鏁?
```python
net[0].weight.data.normal_(0, 0.01)
net[0].bias.data.fill_(0) # 涓嬪垝绾胯〃绀哄師鍦颁慨鏀?```

## 5. 鎹熷け鍑芥暟

```python
loss = nn.MSEloss() # Mean Squared Errror 鍧囨柟璇樊锛屾爣閲忔崯澶?```

## 6. 浼樺寲鍣?torch.optim.SGD

```python
trainer = torch.optim.SGD(net.parameters(), lr=0.03)
```

## 7. 瀹屾暣璁粌寰幆

```python
num_epochs = 3
data_iter = load_array((features, labels), batch_size=10)

for epoch in range(num_epochs):
	for X, y in data_iter:
		l = loss (net(X), y)

		trainer.zero_grad() # 娓呴櫎涓婁竴鎵规搴?		l.bvackward()       # 鍙嶅悜浼犳挱
		trainer.step()      # 鏇存柊姊害

	l = loss(net(features), labels)
	print(f'epoch {epoch+1}, loss {l:f}')
```

