# GPU

鎬庝箞鎶婂紶閲忓拰妯″瀷鏀惧埌 GPU 涓婅绠椼€?GPU锛氬苟琛岃绠楋紝閫傚悎澶ч噺鐭╅樀涔樻硶銆?
## 1. device

```python
import torch

torch.device('cpu') # 琛ㄧず CPU 璁惧
torch.device('cuda:0') # 琛ㄧず绗?0 鍧?GPU

torch.cuda.device_count()  # 杩斿洖 GPU 鏁伴噺
```

## 2. 閫氬父鍐欐硶

```python
device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')

net = net.to(device)

for X, y in train_iter:
    X = X.to(device)
    y = y.to(device)

    y_hat = net(X)            # 鍓嶅悜浼犳挱锛屾鏃舵ā鍨嬪拰鏁版嵁鍦ㄥ悓涓€璁惧
    l = loss(y_hat, y)

    optimizer.zero_grad()
    l.backward()
    optimizer.step()
```

NumPy 鍙兘澶勭悊 CPU 涓婄殑鏁版嵁銆傚鏋滃紶閲忓湪 GPU 涓婏紝涓嶈兘鐩存帴numpy銆?
```python
x_np = x_gpu.detach().cpu().numpy()
```

## 3. 淇濆瓨鍒癱pu

```python
state_dict = torch.load('model.pth', map_location='cpu')  # 淇濆瓨鍒癱pu

model.load_state_dict(state_dict)                         # 鍔犺浇鍙傛暟
```

