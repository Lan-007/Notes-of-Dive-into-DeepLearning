# 鏉ヨ嚜Transformers鐨勫弻鍚戠紪鐮佸櫒琛ㄧず锛圔ERT锛?
涓婁笅鏂囨晱鎰燂細
浼犵粺璇嶅悜閲? word2vec銆丟love )鐨勯棶棰橈細涓€涓瘝涓嶇鍑虹幇鍦ㄤ粈涔堜笂涓嬫枃閲岋紝鍚戦噺閮戒竴鏍枫€?BERT锛氬悓涓€涓瘝鍦ㄤ笉鍚屼笂涓嬫枃涓紝鍙互寰楀埌涓嶅悓鐨勫悜閲忚〃绀恒€?
鈥滃弻鍚戔€濓細姣忎釜浣嶇疆鐨?token 琛ㄧず锛岄兘铻嶅悎宸﹀彸涓よ竟鐨勪笂涓嬫枃淇℃伅銆?
BERT浣跨敤**Transformer Encoder**鍘熷洜锛欵ncoder 杈撳叆涓€鏁存鏂囨湰锛岄€氳繃鑷敞鎰忓姏鏈哄埗璁╂瘡涓?token 鍏虫敞鍏朵粬 token 锛岃緭鍑烘瘡涓?token 鐨勪笂涓嬫枃琛ㄧず銆傝繖涓€鐗圭偣闈炲父閫傚悎鏂囨湰鐞嗚В浠诲姟銆?
|妯″瀷|涓婁笅鏂囨柟鍚憒涓嬫父浣跨敤鏂瑰紡|
|---|---|---|
|word2vec/GloVe|鏃犱笂涓嬫枃|鍥哄畾璇嶅悜閲弢
|ELMo|鍙屽悜|閫氬父鍐荤粨妯″瀷锛屽姞鍏ヤ换鍔′笓鐢ㄦ灦鏋剕
|鏃╂湡 GPT|浠庡乏鍚戝彸|缁熶竴棰勮缁冧富骞插苟寰皟|
|BERT|鍙屽悜|缁熶竴棰勮缁冧富骞插苟寰皟|

### 1. 杈撳叆锛歵oken embedding + segment embedding + position embedding

Token Embedding锛歵oken鏈韩鐨勫悜閲?Segment Embedding锛氫笉鍚屽彞瀛愪笉鍚岀殑segment embedding
Position Embedding锛氫綅缃甧mbedding

BERT杈撳叆閲屼袱涓壒娈婄殑token锛歚[CLS]鍜孾SEP]`

$eg:$ `[CLS] I love deep learning [SEP]`

`[CLS]` 鐨勬渶缁堣緭鍑哄悜閲忛€氬父鐢ㄦ潵浠ｈ〃鏁村彞璇濓紝鍙互鐢ㄤ簬鏂囨湰鍒嗙被銆?`[SEP]` 鐢ㄦ潵鍒嗛殧鍙ュ瓙

```python
class BERTEncoder(nn.Module):
    """BERT缂栫爜鍣?""

    def __init__(
        self,
        vocab_size,
        num_hiddens,
        norm_shape,
        ffn_num_input,
        ffn_num_hiddens,
        num_heads,
        num_layers,
        dropout,
        max_len=1000,
        key_size=768,
        query_size=768,
        value_size=768,
        **kwargs
    ):
        super(BERTEncoder, self).__init__(**kwargs)

        self.token_embedding = nn.Embedding(
            vocab_size,
            num_hiddens
        )

        self.segment_embedding = nn.Embedding(
            2,
            num_hiddens
        )

        self.blks = nn.Sequential()

        for i in range(num_layers):
            self.blks.add_module(
                f"{i}",
                d2l.EncoderBlock(
                    key_size,
                    query_size,
                    value_size,
                    num_hiddens,
                    norm_shape,
                    ffn_num_input,
                    ffn_num_hiddens,
                    num_heads,
                    dropout,
                    True
                )
            )

        self.pos_embedding = nn.Parameter(
            torch.randn(
                1,
                max_len,
                num_hiddens
            )
        )
	def forward(
		self,
		tokens,
		segments,
		valid_lens
		):
		X = (self.token_embedding(tokens) + self.segment_embedding(segments))

		X = X + self.pos_embedding.data[:, :X.shape[1], :]

		for blk in self.blks:
			X = blk(X, valid_lens)

		return X
```

### 2. 璁粌浠诲姟锛氳嚜鍑忕洃鐫ｅ涔?
#### 1锛夋帺钄借瑷€妯″瀷 MLM锛圡asked Language Modeling锛?
闅忔満閬綇鍙ュ瓙涓殑涓€浜?token锛岃妯″瀷鏍规嵁涓婁笅鏂囨妸瀹冧滑棰勬祴鍑烘潵銆?
BERT 闅忔満閫夊彇杈撳叆涓害 15% 鐨勬櫘閫氳瘝鍏冧綔涓洪娴嬬洰鏍囥€傚浜庡凡缁忚閫変腑鐨?15% 璇嶅厓锛?
BERT 璁粌鏃朵細闅忔満閫夋嫨涓€閮ㄥ垎 token锛屾瘮濡?15%锛屽杩欎簺琚€変腑鐨?token锛屽鐞嗘柟寮忛€氬父鏄細

|     澶勭悊鏂瑰紡     | 姣斾緥  |        渚嬪瓙        |
| :----------: | :-: | :--------------: |
| 鏇挎崲鎴?`[MASK]` | 80% | `deep -> [MASK]` |
| 鏇挎崲鎴愰殢鏈?token  | 10% | `deep -> apple`  |
|     淇濇寔涓嶅彉     | 10% |  `deep -> deep`  |
鍥犱负鐪熷疄杈撳叆涓紝涓嶄細鍑虹幇 `[MASK]`锛屾墍浠ヨ繘琛岄殢鏈烘浛鎹㈠拰淇濇寔涓嶅彉銆?
```python
class MaskLM(nn.Module):
    """BERT鐨勬帺钄借瑷€妯″瀷浠诲姟"""

    def __init__(
        self,
        vocab_size,
        num_hiddens,
        num_inputs=768,
        **kwargs
    ):
        super(MaskLM, self).__init__(**kwargs)

        self.mlp = nn.Sequential(
            nn.Linear(
                num_inputs,
                num_hiddens
            ),
            nn.ReLU(),
            nn.LayerNorm(num_hiddens),
            nn.Linear(
                num_hiddens,
                vocab_size
            )
        )

	def forward(self, X, pred_positions):
	    num_pred_positions = pred_positions.shape[1]

	    pred_positions = pred_positions.reshape(-1)

	    batch_size = X.shape[0]

	    batch_idx = torch.arange(0, batch_size)

	    batch_idx = torch.repeat_interleave( batch_idx, num_pred_positions)

	    masked_X = X[batch_idx, pred_positions]

	    masked_X = masked_X.reshape((batch_size,num_pred_positions,-1) )

	    mlm_Y_hat = self.mlp(masked_X)

    return mlm_Y_hat

loss = nn.CrossEntropyLoss( reduction='none' )
```

### 2) 涓嬩竴鍙ラ娴?NSP (Next Sentence Prediction)

鍒ゆ柇绗簩涓彞瀛愭槸涓嶆槸绗竴涓彞瀛愮殑涓嬩竴鍙ャ€?
鐢熸垚棰勮缁冩暟鎹椂锛?- 50% 浣跨敤鐪熷疄杩炵画鍙ュ瓙锛?- 50% 鎶婄浜屽彞璇濇浛鎹负闅忔満鍙ュ瓙銆?
```python
class NextSentencePred(nn.Module):
    """BERT鐨勪笅涓€鍙ラ娴嬩换鍔?""

    def __init__(self, num_inputs, **kwargs):
        super(
            NextSentencePred,
            self
        ).__init__(**kwargs)

        self.output = nn.Linear(
            num_inputs,
            2
        )

    def forward(self, X):
        return self.output(X)

loss = nn.CrossEntropyLoss( reduction='none' )
```

## 3. BERT妯″瀷

```python
class BERTModel(nn.Module):
    """BERT妯″瀷"""

    def __init__(
        self,
        vocab_size,
        num_hiddens,
        norm_shape,
        ffn_num_input,
        ffn_num_hiddens,
        num_heads,
        num_layers,
        dropout,
        max_len=1000,
        key_size=768,
        query_size=768,
        value_size=768,
        hid_in_features=768,
        mlm_in_features=768,
        nsp_in_features=768
    ):
        super(BERTModel, self).__init__()

		# 缂栫爜鍣細璐熻矗姣忎釜浣嶇疆鐨勪笂涓嬫枃鍖栬〃绀?        self.encoder = BERTEncoder(
            vocab_size,
            num_hiddens,
            norm_shape,
            ffn_num_input,
            ffn_num_hiddens,
            num_heads,
            num_layers,
            dropout,
            max_len=max_len,
            key_size=key_size,
            query_size=query_size,
            value_size=value_size
        )

		# 澶勭悊 <cls> 鐨勯殣钘忕姸鎬?        self.hidden = nn.Sequential(
            nn.Linear(
                hid_in_features,
                num_hiddens
            ),
            nn.Tanh()
        )
		# 涓や釜浠诲姟澶?        self.mlm = MaskLM(
            vocab_size,
            num_hiddens,
            mlm_in_features
        )

        self.nsp = NextSentencePred(
            nsp_in_features
        )

	def forward(
	    self,
	    tokens,
	    segments,
	    valid_lens=None,
	    pred_positions=None
	):
	    encoded_X = self.encoder(
	        tokens,
	        segments,
	        valid_lens
	    )

	    if pred_positions is not None:
	        mlm_Y_hat = self.mlm(
	            encoded_X,
	            pred_positions
	        )
	    else:
	        mlm_Y_hat = None

	    nsp_Y_hat = self.nsp(
	        self.hidden(
	            encoded_X[:, 0, :]
	        )
	    )

	    return (
	        encoded_X,
	        mlm_Y_hat,
	        nsp_Y_hat
	    )
```

### 4. BERT 鐨勯璁粌涓庡井璋?
#### 1. 棰勮缁冮樁娈?
$杈撳叆璇枡搴揬to鏋勯€燤LM鍜孨SP鏍囩\to璁粌BERT\space Encoder$

#### 2. 寰皟闃舵

閽堝鍏蜂綋浠诲姟澧炲姞绠€鍗曡緭鍑哄眰銆?
鏂囨湰鍒嗙被锛?<cls>琛ㄧず \to 绾挎€у垎绫诲眰 鈫?鎯呮劅绫诲埆$

鍛藉悕瀹炰綋鍒嗙被锛?姣忎釜璇嶅厓鐨勮〃绀?\to 閫愯瘝鍒嗙被灞?\to 浜哄悕/鍦扮偣/缁勭粐/鍏朵粬$

闂瓟锛?姣忎釜璇嶅厓鐨勮〃绀?\to 棰勬祴绛旀寮€濮嬩綅缃拰缁撴潫浣嶇疆$

