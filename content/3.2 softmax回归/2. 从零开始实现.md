# 浠庨浂寮€濮嬪疄鐜?
### 涓€銆佸浘鍍忓垎绫绘暟鎹泦

Fashion-MNIST 鏄湇瑁呭浘鐗囧垎绫绘暟鎹泦锛屽叡 10 绫伙細

```text
T 鎭ゃ€佽￥瀛愩€佸琛€佽繛琛ｈ銆佸濂椼€佸噳闉嬨€佽‖琛€佽繍鍔ㄩ瀷銆佸寘銆佺煭闈?
璁粌闆嗭細60,000 寮?娴嬭瘯闆嗭細10,000 寮?姣忓紶鍥剧墖锛?8 脳 28 鐏板害鍥?绫诲埆鏁帮細10
```

#### 1. 璇诲彇骞惰浆鎹㈠浘鐗?
```python
import torchvision
from torchvision import transforms

trans = transforms.ToTensor()
train_data = torchvision.datasets.FashionMNIST(
	root = "./data",
	train=True,
	transform=trans,
	download=True
)
# (X, y)
test_data = torchvision.datasets.FashionMNIST(
	root="../data",
	train=False,
	transform=trans,
	download=True
)
# 鍘熷鍥剧墖閫氬父鏄?PIL 鍥惧儚锛屽儚绱犲€艰寖鍥存槸0-255
# 缁忚繃ToTensor()鍚庯細0.0-1.0, 鍗曞紶鍥剧墖褰㈢姸锛?1,28,28)
```

#### 2. DataLoader锛氬皬鎵归噺鏁版嵁杩唬鍣?
```python
from torch.utils import data

batch_size = 256

train_iter = data.DataLoader(
	train_data,
	batch_size=batch_size,
	shuffle=True,
)
```

## 浜屻€佷粠闆跺疄鐜皊oftmax鍥炲綊

softmax鏄叏杩炴帴绾挎€фā鍨嬶紝鍙帴鍙椾竴缁寸壒寰佸悜閲忥紝鍒欓渶瑕佸睍骞冲浘鍍忎负784(姝ゆ椂鏁堟灉鏈夐檺锛屽洜涓哄拷鐣ヤ簡鈥濈浉閭诲儚绱犻€氬父鐩稿叧鈥滐紝CNN浼氳В鍐宠繖涓棶棰?

#### 1. 璇诲彇鏁版嵁闆?
```python
import torch
from d2l import torch as d2l

batch_size = 256
train_iter, test_iter = d2l.load_data_fashion_mnist(batch_size)
```

#### 2. 鍒濆鍖栨ā鍨嬪弬鏁?
鍓嶅悜浼犳挱锛?
```text
X_flat:     (batch_size, 784)
W:          (784, 10)
b:          (10,)
logits:     (batch_size, 10)
```

$$
\mathbf{O}=\mathbf{X}\mathbf{W}+b
$$

```python
num_inputs = 784
num_outputs = 10

w = torch.normal(0, 0.01, size =(num_inputs, num_outputs), requires_grad=True)
b = torch.zeros(num_outputs, requires_grad=True)
```

#### 3. softmax鍑芥暟

```python
def softmax(X):
	X_exp = torch.exp(X)
	partition = X_exp.sum(1, keepdim=True)
	return X_exp/partition
```

#### 4. 妯″瀷鍑芥暟

```python
def net(X):
	return softmax(torch.matmul(X.reshape((-1, w.shape[0])), w) + b)
```

#### 5. 浜ゅ弶鐔垫崯澶?
```python
def cross_entropy(y_hat, y):
	return -torch.log(y_hat[range(len(y_hat)), y])
```

涓句緥锛?
```python
y = torch.tensor([0, 2])

y_hat = torch.tensor([
    [0.1, 0.3, 0.6],
    [0.3, 0.2, 0.5]
])

y_hat[range(len(y_hat)), y] 绛変环浜?y_hat[[0, 1], [0, 2]]
缁撴灉锛歔0.1, 0.5]
```

#### 6. 鍑嗙‘鐜?
```python
def accuracy(y_hat, y):
	if len(y_hat.shape) > 1 and y_hat.shape[1] > 1: # 绫诲埆澶т簬1
		y_hat = y_hat.argmax(axis=1)                # 姣忚鎵炬渶澶у€肩殑浣嶇疆

	cmp = y_hat.type(y.dtype) == y                  # 杞崲绫诲瀷(eg:float杞负int)
	return float(cmp.type(y.type).sum())            # 琛ㄧず棰勬祴姝ｇ‘涓暟
```

#### 7. 璇勪及鏁翠釜鏁版嵁闆?
```python
def evaluate_accuracy(net, data_iter):
    """璁＄畻鍦ㄦ寚瀹氭暟鎹泦涓婃ā鍨嬬殑绮惧害"""

    if isinstance(net, torch.nn.Module):
        net.eval()                  # 濡傛灉妯″瀷鏄疨yTorch鐨刵n.Module,灏卞垏鎹㈠埌璇勪及妯″紡

    metric = Accumulator(2)         # 涓や釜绱姞鍣?
    with torch.no_grad():
        for X, y in data_iter:
            metric.add(accuracy(net(X), y), y.numel())

    return metric[0] / metric[1]
```

#### 8. 绱姞鍣?
```python
class Accumulator:
    """鍦╪涓彉閲忎笂绱姞"""

    def __init__(self, n):
        self.data = [0.0] * n

    def add(self, *args):
        self.data = [a + float(b) for a, b in zip(self.data, args)]

    def reset(self):
        self.data = [0.0] * len(self.data)

    def __getitem__(self, idx):
        return self.data[idx]
```

#### 9. 璁粌涓€涓猠poch

```python
def train_epoch_ch3(net, train_iter, loss, updater):
    """璁粌妯″瀷涓€涓凯浠ｅ懆鏈?""

    if isinstance(net, torch.nn.Module):
        net.train()

    metric = Accumulator(3)

    for X, y in train_iter:
        y_hat = net(X)                      # 鍓嶅悜浼犳挱
        l = loss(y_hat, y)                  # 璁＄畻loss

        if isinstance(updater, torch.optim.Optimizer):
            updater.zero_grad()
            l.mean().backward()
            updater.step()
        else:
            l.sum().backward()
            updater(X.shape[0])             # 鍙嶅悜浼犳挱銆佹洿鏂板弬鏁?
        metric.add(float(l.sum()), accuracy(y_hat, y), y.numel()) #

    return metric[0] / metric[2], metric[1] / metric[2]
```

#### 10. 璁粌澶氫釜epoch

```python
def train_ch3(net, train_iter, test_iter, loss, num_epochs, updater):
    """璁粌妯″瀷"""

    animator = Animator(
        xlabel='epoch',
        xlim=[1, num_epochs],
        ylim=[0.3, 0.9],
        legend=['train loss', 'train acc', 'test acc']
    )

    for epoch in range(num_epochs):
        train_metrics = train_epoch_ch3(net, train_iter, loss, updater)
        test_acc = evaluate_accuracy(net, test_iter)
        animator.add(epoch + 1, train_metrics + (test_acc,))

    train_loss, train_acc = train_metrics

    assert train_loss < 0.5, train_loss
    assert train_acc <= 1 and train_acc > 0.7, train_acc
    assert test_acc <= 1 and test_acc > 0.7, test_acc
```

#### 11. 浼樺寲鏂规硶锛歴gd

```python
lr = 0.1
def updater(batch_size):
    return d2l.sgd([W, b], lr, batch_size)
```

