# 延后初始化

有些层在创建的时候，还不知道输入维度是多少，所以暂时不真正创建参数；等第一次看到输入数据时，再根据输入形状自动初始化参数。

PyTorch 提供了懒加载层，比如：`nn.LazyLinear(256)` 意思是输出维度是256

```python
net = nn.Sequential(
	nn.LazyLinear(256), # 只写输出 256 维，输入维度之后自动推断
	nn.ReLU(),
	nn.LazyLinear(10) # 只写输出 10 维，输入维度之后自动推断
)
```

第一次前向传播之后，再根据输入形状自动初始化参数。
