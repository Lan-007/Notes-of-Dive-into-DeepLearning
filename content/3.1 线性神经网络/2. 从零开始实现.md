# 浠庨浂寮€濮嬪疄鐜?
```text
鐢熸垚鏁版嵁
鈫?璇诲彇batch
鈫?瀹氫箟妯″瀷
鈫?瀹氫箟loss
鈫?璁＄畻棰勬祴
鈫?璁＄畻姊害
鈫?鏇存柊鍙傛暟
鈫?閲嶅璁粌
```

### 1. 鐢熸垚鍚堟垚鏁版嵁闆?
鍋囪鐪熷疄瑙勫緥鏄細

$$
\mathbf{w}=\begin{bmatrix} 2\\ -3.4 \end{bmatrix}, \quad b=4.2
$$

鐢熸垚鏁版嵁锛?
```python
def synthetic_data(w, b, num_examples):
	"""鐢熸垚 y = Xw + b + 鍣０"""

	X = torch.normal(0,1,(num_examples, len(w))) # 姝ｆ€佸垎甯?
	y = torch.matmul(X, w) + b

	y += torch.normal(0,0.01, y.shape)

	return X, y.reshape((-1,1)) #璋冩暣涓哄垪鍚戦噺锛屼笌妯″瀷杈撳嚭鍖归厤
```

### 2. 灏忔壒閲忔暟鎹凯浠ｅ櫒锛?
```python
def data_iter(batch_size, features, labels):
	鈥濃€?鎶婃暣涓缁冩暟鎹殢鏈烘墦涔憋紝姣忔杩斿洖batch涓牱鏈€濃€溾€?
	num_examples = len(features)

	indices = list(range(num_examples))  # 鍒楄〃涓殑鍏冪礌涓虹紪鍙?
	random.shuffle(indices)              # 鎵撲贡缂栧彿

	for i in range(0, num_examples, batch_size):
		batch_indices = torch.tensor(indices[i:min(i + batch_size, num_examples)])
		yield features[batch_indices], labels[batch_indices]
		#涓嶄細涓€娆¤繑鍥炴墍鏈塨atch锛岃€屾槸姣忔瑕佹暟鎹椂鎵嶇敓鎴愪笅涓€涓猙atch
```

### 3. 鍒濆鍖栧弬鏁?
```python
w = torch.normal(0,0.01, size=(2,1),requires_grad=True)

b = torch.zeros(1, requires_grad=True)
```

### 4. 瀹氫箟妯″瀷

```python
def linreg(X, w, b):
	"""绾挎€у洖褰掓ā鍨?""
	return torch.matmul(X, w)+b
```

$$
\hat{y} = \mathbf{X}\mathbf{w}+b
$$

### 5. 瀹氫箟骞虫柟鎹熷け

```python
def squard_loss(y_hat, y):
	""鍧囨柟鎹熷け鈥溾€?	return (y_hat - y.reshape(y_hat.shape))**2 /2
```

y.reshape: 闃叉y.shape涓?10,)涓?10,1)瑙﹀彂骞挎挱浜х敓(10,10)缁撴灉

### 6. 浼樺寲绠楁硶锛氶殢鏈烘搴︿笅闄?
```python
def sgd(params, lr, batch_size):
	"""灏忔壒閲忛殢鏈烘搴︿笅闄?""
	with torch.no_grad():
		for param in params:
			param -= lr * param.grad / batch_size
			param.grad.zero_()
```

$$
\theta\leftarrow \theta-\frac{\eta}{B}\nabla _{\theta}L_{batch}
$$

杩欓噷鎹熷け鍦ㄨ缁冨惊鐜腑浣跨敤 `l.sum()` 姹囨€汇€?濡傛灉鐢╜l.mean().backward()` 灏变笉鑳藉啀闄や竴娆atch_size

## 7. 瀹屾暣璁粌寰幆

```python
lr = 0.03
num_epochs = 3
net = linreg
loss = squard_loss

for epoch in range(num_epochs):
	for X,y in data_iter(batch_size, features, labels):

		l = loss(net(X, w, b), y)

		l.sum().backward()

		sgd([w, b], lr, batch_size)

	with torch.no_grad():
		train_l = loss(net(features, w, b), labels)
		print(f'epoch {epoch+1}, loss {float(train_l.mean()):f}')
```

